{
  "name": "@byhuz/spine-ts",
  "version": "0.1.0",
  "description": "The spine-ts runtime provides functionality to load and manipulate [Spine](http://esotericsoftware.com) skeletal animation data using TypeScript and JavaScript. spine-ts is split up into multiple modules:",
  "files": [
    "build"
  ],
  "scripts": {
    "deploy": "npm publish --access public"
  }
}